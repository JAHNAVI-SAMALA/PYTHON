nums = list(map(int,input().split()))
def SelectionSortAlg(nums):
    for i in range(0,len(nums)):
        minimum = i
        for j in range(i+1,len(nums)):
            if nums[j] < nums[minimum]:
                minimum = j
        nums[i],nums[minimum] = nums[minimum],nums[i]
    return nums
print(SelectionSortAlg(nums)
