s = input()
def longestPrefix(s):
    s1 = s.split()
    if not s1:
        return ""
    prefix = s1[0]
    for word in s1[1:]:
        while not word.startswith(prefix):
            prefix = prefix[:-1]
            if not prefix:
                return ""
    return prefix
print(longestPrefix(s))    
