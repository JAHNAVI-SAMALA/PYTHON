n = int(input())
arr = list(map(int,input().split()))
def SumOfOddinArr(n,arr):
    total = 0
    for i in range(0,n):
        if arr[i]%2!=0:
            total += arr[i]
    return total
print(SumOfOddinArr(n,arr))
            
